// Single place that knows where the vendored three.js example
// modules live (the 'three' bare specifier itself is mapped by the
// importmap in index.html).
export { GLTFLoader } from '../vendor/loaders/GLTFLoader.js';
export { RoundedBoxGeometry } from '../vendor/geometries/RoundedBoxGeometry.js';
